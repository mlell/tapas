reads="$read_dir/all_mut_{mut}.fastq";
out="run/{index}";
log="log/{index}.log";

bwa aln
    -n {n} -k {k}
    $bwa_index
    $reads
    > ${out}.sai 2>$log &&

bwa samse $bwa_index ${out}.sai $reads > ${out}.sam 2>>$log &&

gzip ${out}.sam 2>>$log && 

rm ${out}.sai 2>>$log
