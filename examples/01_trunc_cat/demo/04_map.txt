# don't delete data if this file should be executed non-interactively
set -ueC

rootdir="$(readlink -f ../ )"
d_dsc="$rootdir/data/map_par"
scd="$rootdir/scripts"

s_mapping="$scd/map/mapping_run.sh"
s_mcall="$scd/general/mcall.py"

mkdir 04_out
cd 04_out

export bwa_index="$rootdir/data/ref/fcatus/fcatus.fasta"
export read_dir="$rootdir/demo/03_out"

$s_mapping $d_dsc out 
cd out
$s_mcall --status --log-time time.log -c calls -t 2


# vim:ft=sh


